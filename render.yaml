services:
  # --- API Gateway (The "Front Door") ---
  - type: web
    name: funstakes-gateway
    env: node
    plan: free
    rootDir: backend-v2
    buildCommand: corepack enable && pnpm install && pnpm --filter @repo/gateway build
    startCommand: pnpm --filter @repo/gateway start
    domains:
      - api.funstakes.net
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PORT
        value: "8000"
      - fromGroup: funstakes-common-secrets

  # --- Auth Service ---
  - type: web
    name: funstakes-auth
    env: node
    plan: free
    rootDir: backend-v2
    buildCommand: corepack enable && pnpm install
    startCommand: pnpm --filter @repo/auth-service start
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PORT
        value: "8080"
      - fromGroup: funstakes-common-secrets
      - fromGroup: funstakes-communication-secrets

  # --- Post Service ---
  - type: web
    name: funstakes-post
    env: node
    plan: free
    rootDir: backend-v2
    buildCommand: corepack enable && pnpm install
    startCommand: pnpm --filter @repo/post-service start
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PORT
        value: "8081"
      - fromGroup: funstakes-common-secrets

  # --- User Service ---
  - type: web
    name: funstakes-user
    env: node
    plan: free
    rootDir: backend-v2
    buildCommand: corepack enable && pnpm install
    startCommand: pnpm --filter @repo/user-service start
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PORT
        value: "8082"
      - fromGroup: funstakes-common-secrets

  # --- Worker Service ---
  - type: web
    name: funstakes-worker
    env: node
    plan: free
    rootDir: backend-v2
    buildCommand: corepack enable && pnpm install
    startCommand: pnpm --filter @repo/worker-service start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PORT
        value: "8083"
      - fromGroup: funstakes-common-secrets
      - fromGroup: funstakes-communication-secrets

  # --- Admin Service ---
  - type: web
    name: funstakes-admin
    env: node
    plan: free
    rootDir: backend-v2
    buildCommand: corepack enable && pnpm install
    startCommand: pnpm --filter @repo/admin-service start
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PORT
        value: "8084"
      - fromGroup: funstakes-common-secrets
