"use client";
// default page
export const defaultPage = {
  title: "home",
  path: "/web/home",
};

export const excludedRoutes = {
  auth: ["/auth/login", "/auth/signup"],
  others: ["/web/home"],
};
