services:
  # --- Gateway Service ---
  gateway-service:
    build:
      context: .
      dockerfile_inline: |
        FROM node:20-alpine
        RUN apk add --no-cache libc6-compat
        WORKDIR /app/backend-coolify
        RUN corepack enable && corepack prepare pnpm@latest --activate
        COPY . /app
        RUN pnpm install && pnpm --filter @repo/gateway build
        # Using pnpm exec because tsx is a devDependency
        CMD ["pnpm", "--filter", "@repo/gateway", "exec", "node", "--import", "tsx", "--env-file=/app/.env.shared", "dist/index.js"]
    volumes:
      # Format: [Host Path]:[Container Path]:[Mode]
      - /data/funstakes/.env.shared:/app/.env.shared:ro
    environment:
      - GATEWAY_PORT=8000
      - GATEWAY_URL={{project.BACKEND_URL}}
      - GOOGLE_APPLICATION_CREDENTIALS={{project.GOOGLE_APPLICATION_CREDENTIALS}}
    labels:
      - "coolify.name=funstakes-backend-gateway"
      - "coolify.exposed_port=8000"
    networks:
      - coolify

  # --- Account Service ---
  account-service:
    build:
      context: .
      dockerfile_inline: |
        FROM node:20-alpine
        RUN apk add --no-cache libc6-compat
        WORKDIR /app/backend-coolify
        RUN corepack enable && corepack prepare pnpm@latest --activate
        COPY . /app
        RUN pnpm install && pnpm --filter @repo/account-service build
        CMD ["pnpm", "--filter", "@repo/account-service", "exec", "node", "--import", "tsx", "--env-file=/app/.env.shared", "dist/index.js"]
    volumes:
      - /data/funstakes/.env.shared:/app/.env.shared:ro
    environment:
      - ACCOUNT_PORT=8080
      - GOOGLE_APPLICATION_CREDENTIALS={{project.GOOGLE_APPLICATION_CREDENTIALS}}
    labels:
      - "coolify.name=funstakes-backend-account"
    networks:
      - coolify

  # --- Post Service ---
  post-service:
    build:
      context: .
      dockerfile_inline: |
        FROM node:20-alpine
        RUN apk add --no-cache libc6-compat
        WORKDIR /app/backend-coolify
        RUN corepack enable && corepack prepare pnpm@latest --activate
        COPY . /app
        RUN pnpm install && pnpm --filter @repo/post-service build
        CMD ["pnpm", "--filter", "@repo/post-service", "exec", "node", "--import", "tsx", "--env-file=/app/.env.shared", "dist/index.js"]
    volumes:
      - /data/funstakes/.env.shared:/app/.env.shared:ro
    environment:
      - POST_PORT=8081
      - GOOGLE_APPLICATION_CREDENTIALS={{project.GOOGLE_APPLICATION_CREDENTIALS}}
    labels:
      - "coolify.name=funstakes-backend-post"
    networks:
      - coolify

  # --- Worker Service ---
  worker-service:
    build:
      context: .
      dockerfile_inline: |
        FROM node:20-alpine
        RUN apk add --no-cache libc6-compat
        WORKDIR /app/backend-coolify
        RUN corepack enable && corepack prepare pnpm@latest --activate
        COPY . /app
        RUN pnpm install && pnpm --filter @repo/worker-service build
        CMD ["pnpm", "--filter", "@repo/worker-service", "exec", "node", "--import", "tsx", "--env-file=/app/.env.shared", "dist/index.js"]
    volumes:
      - /data/funstakes/.env.shared:/app/.env.shared:ro
    environment:
      - WORKER_PORT=8083
      - GOOGLE_APPLICATION_CREDENTIALS={{project.GOOGLE_APPLICATION_CREDENTIALS}}
    labels:
      - "coolify.name=funstakes-backend-worker"
    networks:
      - coolify

  # --- Admin Service ---
  admin-service:
    build:
      context: .
      dockerfile_inline: |
        FROM node:20-alpine
        RUN apk add --no-cache libc6-compat
        WORKDIR /app/backend-coolify
        RUN corepack enable && corepack prepare pnpm@latest --activate
        COPY . /app
        RUN pnpm install && pnpm --filter @repo/admin-service build
        CMD ["pnpm", "--filter", "@repo/admin-service", "exec", "node", "--import", "tsx", "--env-file=/app/.env.shared", "dist/index.js"]
    volumes:
      - /data/funstakes/.env.shared:/app/.env.shared:ro
    environment:
      - ADMIN_PORT=8084
      - GOOGLE_APPLICATION_CREDENTIALS={{project.GOOGLE_APPLICATION_CREDENTIALS}}
    labels:
      - "coolify.name=funstakes-backend-admin"
    networks:
      - coolify

networks:
  coolify:
    external: true
