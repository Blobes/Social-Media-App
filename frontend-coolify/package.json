{
  "name": "funstakes",
  "private": true,
  "scripts": {
    "dev": "dotenv -e .env.development -- turbo run dev",
    "build": "dotenv -e .env.production -- turbo run build",
    "start": "dotenv -e .env.development -- turbo run start",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "check-types": "turbo run check-types",
    "clean": "turbo clean && rm -rf node_modules **/.next"
  },
  "devDependencies": {
    "dotenv-cli": "catalog:default",
    "next": "catalog:",
    "prettier": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.30.1",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "overrides": {
      "minimatch": ">=9.0.7"
    }
  }
}
