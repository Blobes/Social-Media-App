{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "globalDotEnv": [".env.development", ".env.production"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "build/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "inputs": ["$TURBO_DEFAULT$", ".env.development"]
    },
    "format": {
      "cache": true,
      "outputs": []
    }
  }
}
